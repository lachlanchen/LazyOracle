# /etc/lazyoracle/gateway.env  (root:lazyoracle-web, mode 640). Providers are tried in this order.
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-chat
# LazyEdge route to the workstation LocalLLM (full chat-completions URL + client token)
LAZYEDGE_URL=
LAZYEDGE_TOKEN=
LAZYEDGE_MODEL=localllm-pocket
